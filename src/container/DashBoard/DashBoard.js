import React from 'react';
import {
    View,
} from 'react-native';

function DashBoard(props) {
    const { navigation } = props;

    return (
        <View>

        </View>
    );
}

export default DashBoard;